# Color Kit

`colorkit` is a lightweight `#[no_std]` color crate for Rust. It provides an easy to use
and strongy typed conversions between color spaces. It also provides layout and quantization
tools for working with with pixel data.